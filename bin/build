#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd ${DIR}/..
REV=$(git describe --tags)
PROJECT=${{ gar_docker_registry }}/frontend-monitoring
docker build --tag ${PROJECT}:${REV} .
